[package]
name = "ombudsman-core"
version.workspace = true
edition.workspace = true
description = "Shared wire protocol for ombudsman — used by both server and clients"
license = "MIT"

[dependencies]
serde = { workspace = true }
rmp-serde = { workspace = true }
anyhow = { workspace = true }

[lib]
name = "ombudsman_core"
path = "src/lib.rs"